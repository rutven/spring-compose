version: '3.1'

services:
    
    db:
        image: postgres:latest
        restart: always
        ports:
            - "5432:5432"
        environment:
            POSTGRESS_USERNAME: postgres
            POSTGRES_PASSWORD: password
            POSTGRES_DB: blog
